import{AlertCircle,Archive,Check,X}from'lucide-react';import{useEffect,type ReactNode}from'react';
export function PageHeader({eyebrow,title,description,actions}:{eyebrow?:string;title:string;description?:string;actions?:ReactNode}){return <header className="page-header"><div>{eyebrow&&<span className="eyebrow">{eyebrow}</span>}<h1>{title}</h1>{description&&<p>{description}</p>}</div>{actions&&<div className="header-actions">{actions}</div>}</header>}
export function Empty({icon:Icon=Archive,title,description,action}:{icon?:typeof Archive;title:string;description:string;action?:ReactNode}){return <div className="empty"><span><Icon/></span><h3>{title}</h3><p>{description}</p>{action}</div>}
export function Modal({title,children,onClose}:{title:string;children:ReactNode;onClose:()=>void}){useEffect(()=>{const fn=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[onClose]);return <div className="modal-backdrop" role="presentation" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="modal" role="dialog" aria-modal="true" aria-label={title}><header><div><span className="eyebrow">Portfolio record</span><h2>{title}</h2></div><button className="icon" onClick={onClose} aria-label="Close"><X/></button></header>{children}</section></div>}
export function Notice({type='info',children}:{type?:'info'|'success'|'warning'|'error';children:ReactNode}){return <div className={`notice ${type}`}>{type==='success'?<Check/>:<AlertCircle/>}<span>{children}</span></div>}

